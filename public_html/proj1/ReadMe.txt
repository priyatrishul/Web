Html pages can be created using CGI files.If the CGI Program is using parameters passed by the browser like session and cookies, 
they are set to null when logged out.
when Refresh happens CGI uses the cached Parameters to create new session and page rendered becomes valid even when logged out.

In this Assignment only HTML Pages used to Add,Delete,Edit Inventory CGI files are used when Action events are triggered not on Form Action.
